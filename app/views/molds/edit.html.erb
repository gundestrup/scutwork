<%= tabnav :shift_actions %>
<h1><%= t('mold.edit') %></h1>

<%= render :partial => 'form' -%>

<%= link_to t('show'), user_mold_path(@user, @mold) %> |
<%= link_to t('back'), user_molds_path(@user) %>
